# Disk Space Alert

Shell automation that checks disk usage and reports partitions that exceed a selected limit.

## Run
```bash
chmod +x disk_space_alert.sh
./disk_space_alert.sh 80
```

The default limit is 80%. A report is saved in `reports/`.
